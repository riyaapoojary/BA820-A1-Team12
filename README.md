# BA820-A1-Team12
BA820 project repository containing a fully populated EDA notebook (with outputs) for the Hollywood Age Gap dataset (primary) and the SAFI African farm survey dataset (backup), including visualizations, PCA, and KMeans clustering for segmentation.
